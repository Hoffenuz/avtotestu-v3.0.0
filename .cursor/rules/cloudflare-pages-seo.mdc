---
description: Cloudflare Pages middleware, _headers, _redirects o‘zgartirish qoidalari
globs: functions/**/*.{ts,js},public/_headers,public/_redirects,public/_seo/**/*
alwaysApply: false
---

# Middleware / _headers / _seo

## `functions/_middleware.ts`

- User SPA: avvalo `env.ASSETS.fetch('/')`, aks holda `next()` **bir marta**.
- `next()` dan keyin hech qachon yana `next()` / `serveSpaShell` (ichida next) chaqirmang.
- Bot SEO: `ASSETS.fetch('/_seo/.../')` (trailing slash). `/_seo/.../index.html` dan qoching (308).
- Asset javobidan `Cache-Control` / `Age` / `ETag` ni SPA javobiga ko‘chirmang.
- SPA URL javobi: `private, no-store` + `CDN-Cache-Control: no-store`.
- `isStaticAsset`: faqat haqiqiy kengaytmalar (`.js`, `.webp`…). `/belgilar/1.3.1` static emas.
- 404 va 3xx: SPA fallback faqat `ASSETS.fetch('/')` orqali (ikkinchi `next` yo‘q).

## Pages dashboard

- Not found behavior = **Single Page Application** (Settings → Builds).
- `env.ASSETS` Pages Functions da standart binding.

## `public/_headers`

- `/*` = faqat security headerlar (HSTS, CSP, …). Cache-Control yo‘q.
- `no-store`: `/`, `/index.html`, `/auth/*`, `/profile/*`.
- `s-maxage`: `/_seo/*`, JSON, `/assets/*`, rasmlar, sitemap/robots.

## `public/_redirects`

- apex → www 301
- legacy `/tests` → `/test-ishlash`
- `/* /index.html 200` SPA fallback
- SEO shadow `public/mavzuli/index.html` yaratmang

## SEO kontent

- Generatorlar faqat `public/_seo/` ga yozsin.
- Build: `assert-no-spa-shadow` o‘tsin.
- SEO o‘sishi muhim — bot snapshot ni o‘chirmang; faqat keshni to‘g‘ri saqlang.
