# ---------------------------------------------------------------------------
# Cloudflare Pages — redirect qoidalari
# ESLATMA:
#   - SEO snapshot: public/_seo/... (botlar middleware orqali)
#   - SPA route papkalarida index.html BO'LMASLIGI shart (assert-no-spa-shadow)
#   - Middleware: user → /index.html; bot → /_seo/...
# ---------------------------------------------------------------------------

# 1. non-www → www (SEO canonical)
https://avtotestu.uz/* https://www.avtotestu.uz/:splat 301

# 2. Legacy / xato yozilgan URLlar (GSC: 130+ impressions, 0 click)
/tests /test-ishlash 301
/test  /test-ishlash 301

# 3. SPA fallback — shadow HTML yo'q bo'lganda /mavzuli va hokazo shu yerga tushadi
/* /index.html 200
